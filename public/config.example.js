// Copy this file to config.js (same folder) and fill in your Supabase project
// values. config.js is gitignored so your project URL stays out of the repo
// history. Both of these are PUBLIC values (safe to ship to the browser) —
// the anon key is gated by Row Level Security. Never put the service_role key here.
window.GIGCUTE_CONFIG = {
  SUPABASE_URL: 'https://YOUR-PROJECT-ref.supabase.co',
  SUPABASE_ANON_KEY: 'YOUR-ANON-PUBLIC-KEY',
};
